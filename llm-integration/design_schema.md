recap: Goal: design how the LLM workflow feeds the frontend after Phase 5. Current task: deciding on a deterministic UI assembler that joins math outputs and Phase 5 narrative into one DashboardPayload schema. Next: draft dashboard.py with the unified schema and an assemble_dashboard stub. (disable recaps in /config)

use ui_schena_design.md to understand the feature implementation plan.